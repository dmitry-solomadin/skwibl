// Generated by CoffeeScript 1.4.0
(function() {

  $(function() {
    var App, app;
    app = {
      initRemote: function() {
        $("form[data-remote]").on("submit", function() {
          var data, onsuccess;
          data = {};
          $(this).find("input").each(function() {
            return data[$(this).attr("name")] = $(this).val();
          });
          onsuccess = $(this).data("process-submit") ? $(this).data("process-submit") : function() {
            return $("#main-content").html(content);
          };
          $.ajax({
            url: $(this).attr("action"),
            data: data,
            type: "POST",
            success: onsuccess
          });
          return false;
        });
        return $("a[data-remote]").on("click", function() {
          $.ajax({
            url: $(this).attr("href"),
            type: "GET",
            success: function(content) {
              return $("#main-content").html(content);
            }
          });
          return false;
        });
      }
    };
    app.initRemote();
    return App = app;
  });

}).call(this);
