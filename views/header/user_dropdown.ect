<ul class="nav pull-right" data-dropdown-toggler="userDropdownMenu" data-duration="200">
  <li class="dropdown ">
    <a class="dropdown-toggle userDropdown" href="#">
      <div>
        <span class="username"><%= @currentUser().displayName %></span>
        <img src="<%= if @currentUser().picture then @currentUser().picture else '/images/avatar.png' %>">
        <span class="caret"></span>
      </div>
    </a>
  </li>
</ul>